var React = require('react');
var HLRenderer = require('HL7RendererApp');

var MenuApp = React.createClass({
    
    render: function () {
        return (<b>Nothing yet!</b>);
        //return {type: menu, props: { content: this.props.content }}
      }

});
/* Module.exports instead of normal dom mounting */
module.exports = MenuApp;
